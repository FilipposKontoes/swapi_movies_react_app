import styled from "styled-components";

const Button = styled.button`
    background: #FFF;
    border-radius: 5px;
`;

export  default function SortButton () {
    return <Button>
        Sort by...
    </Button>
}
